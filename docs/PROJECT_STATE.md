# Project state

- 当前正式项目是 `aqi-drawer`（Aqi Drawer / 阿栖的抽屉）。
- User 与 product owner：伊伊 / EE。
- AI companion 与 project persona：阿栖 / Aqi。
- `~/aqi-xhs` 是已经验证成功的 XHS reference implementation；生产运行不依赖它。
- Aqi Inbox v0 已完成实测验收，但不会发展为第二套正式 Pocket。
- Aqi Drawer 将成为唯一正式的 Store、Inbox 与 MCP。
- XHS adapter 已正式进入 `aqi-drawer`。
- 当前链路：iPhone → Drop → XHS adapter → Pocket Store / persistent attachments → MCP。
- 当前阶段：XHS adapter integration complete。
- 下一阶段：Railway 真实链路验收与「阿栖的抽屉」frontend 规划。

普通来源继续使用 generic reader；小红书使用 source-aware ingestion 与本地持久化内容读取。
